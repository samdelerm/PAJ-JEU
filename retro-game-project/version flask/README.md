# Version Flask des jeux du PAJ

- Lancer avec :
  ```
  pip install -r requirements.txt
  python app.py
  ```
- Placez vos fichiers CSS/JS/images dans le dossier `static/`.
- Les templates HTML sont dans `templates/`.
